import * as angular from 'angular';
import {MainController} from './app/mainController';
const myApp = angular.module('comtrade',[]).controller('MainController',MainController) ;


/*
let vnos = prompt("Vnesi stevilo!");
let i:number;
for ( i=1; i <= Number(vnos); i++)
{
    if (i%3 === 0 && i%5 === 0) console.log("fizzbuzz");
    else if (i%3 === 0) console.log("fizz");
    else if (i%5 === 0) console.log("buzz");
    else console.log(i);
}
*/


/*********************************************/
/*
let maxi=prompt ("Do koliko bos ugibal?");
let tezavnost = prompt ("Izberi tezavnost! 1-easy, 2-medium, 3 -hard")

function getRandomInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
let poskusov:number = 0;
let nakljucna= getRandomInt(0,Number(maxi));
let vnesel = prompt("Ugani steivilo:");
let 
do
{

    if (Number(vnesel)>nakljucna)
    {
        vnesel = prompt ("prevec! Se enkrat!");
    }
    else if (Number(vnesel)<nakljucna)
    {
        vnesel = prompt ("premalo! Se enkrat!");
    }
    else 
    {
        alert("bingo");
        break;
    }
    poskusov++;
    if ((Number(tezavnost) === 2 && poskusov > 10 ) || (Number(tezavnost) === 3 && poskusov > 5))
    {
        alert("zmanjkalo ti je poskusov");
        break;
    }
}while(1);*/

/*mihec.susnik@gmail.com*/ 

